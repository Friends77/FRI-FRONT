export interface Options {
  value: string | number;
  label: string | number;
}

export interface IPaginationParams {
  size?: number;
}

export interface IPaginationResponse {
  hasNext: boolean;
}
