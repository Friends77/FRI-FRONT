import styled from 'styled-components';

export const Wrapper = styled.div`
  display: flex;
  width: 100%;
  height: 100%;
  overflow-y: auto;
`;

export const Content = styled.div`
  width: calc(100% - 72px);
`;
